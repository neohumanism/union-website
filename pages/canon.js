import React from 'react';
import { Container, Typography } from '@mui/material';
import Link from 'next/link';


const CanonPage = () => {
    return (
        <Container sx={{ marginTop: 4, marginBottom: 4}}>
            <Typography>We have laid out for you the manifesto here. The criteria for canonization are explained there as well.</Typography>
            <Typography>manifesto here</Typography>
        </Container>
    );
};

export default CanonPage;
